# 2026-08-11 开发缺口审计与后续执行计划

## 审计基线

本审计以 `main` 当前代码、路线图、兼容性矩阵、M6/M7/M8/M9 专项计划和 GitHub Actions 证据为准。当前不在本地构建或安装；签名方案继续暂缓。

M7.1d 最新代码提交为 [60564de2151738f91e9b953ff44ad0e42496fb2b](https://github.com/TttXxx36/Open-reader-desktop/commit/60564de2151738f91e9b953ff44ad0e42496fb2b)，对应 CI [31475264033](https://github.com/TttXxx36/Open-reader-desktop/actions/runs/31475264033)、Windows Release [31475774771](https://github.com/TttXxx36/Open-reader-desktop/actions/runs/31475774771) 和 installer smoke [31476524993](https://github.com/TttXxx36/Open-reader-desktop/actions/runs/31476524993)，均已通过。

## 已完成的主要阶段

- M0—M5：工程骨架、本地阅读 MVP、书源协议、单源/多源链路和安全缓存基础已完成。
- M6.0—M6.4：设置入口、外部 JSON/URL 书源预览、TXT/EPUB 内容块、阅读排版设置、首页概览和组件拆分已完成代码切片。
- M7.0—M7.1d：书源元数据保真、导入冲突策略、快照恢复、批量启停/分组/备注/权重、同组拖拽排序已完成；XPath/JavaScript/Cookie/Authorization 仍按安全决策拒绝执行。
- M7.2—M7.5：受限分页策略、取消链、诊断导出、失败历史、请求/规则指标已完成安全首切片；next URL 默认关闭，JavaScript 只停留在评估闸门。
- M8.1—M8.5.5e：TXT/EPUB 安全边界、大文件流式解析、图片/漫画预览、缩略图缓存、取消/重试和位置恢复已完成；PDF/MOBI/AZW 仍只读探测。
- M9.0—M9.2.5：图片序列 SQLite 记录、书架恢复、页级指纹、stale/missing 状态、目录重新关联、SHA-256 复核、超时和取消反馈已完成。
- M9.3.1-a/b/c/d1：封面来源/缓存基础、只读重复候选预览和服务端二次校验已完成；仍不自动合并、不物理删除。

## 剩余未完成阶段

### P0：真实 Windows 手工验收

执行记录模板见 [Windows 手工验收记录模板](windows-manual-acceptance-template.md)。模板中的空白项均视为“未验证”，不得用 CI 结果替代目标环境人工记录。

以下内容不能由 CI 完全代替，仍需在目标 Windows 环境记录：

- 安装版和便携版原地升级、卸载后的数据保留；
- WebView2 缺失、离线、网络错误、权限不足时的可操作提示；
- 中文字体、窄窗口、键盘 Tab 焦点和高对比度可读性；
- URL 书源、本地 JSON 书源、导入预览、冲突策略和恢复快照；
- 图片序列中文/UNC 路径、断目录、取消重新关联、stale 复核和缺页恢复；
- M7.2 追链开关关闭/开启、取消按钮和阅读进度不回退。

### P1：M7 书源治理收尾

- 设计并实现书源快照保留/清理策略，必须有数量、大小和恢复前确认；
- 完善拖拽/批量操作在窄窗口和键盘环境中的可用性；
- 保持 XPath、JavaScript、Cookie、Authorization 和音频书源的明确拒绝，不以“导入成功”冒充“执行成功”。

### P1：M8 内容格式收尾

- EPUB 缺失图片/字体/外链资源的逐项提示，以及有限字体/CSS 白名单扩展；
- PDF 独立渲染、搜索、目录和页码模型评估；
- MOBI/AZW/AZW3 只读解析评估；
- 依赖许可证、供应链、内存上限、受保护内容拒绝和 Windows CI 基线未全部通过前，不引入解析运行时。

### P1：M9.3.1 可撤销重复书合并

- d2：设计并迁移 `0016`，增加生命周期状态、操作记录、来源快照和单跳别名；
- d2 首版只允许纯文本、无正文冲突、无图片序列和无远程封面请求；
- 合并必须预校验、单事务、可回滚，只归档来源书籍，不物理删除、不静默覆盖；
- d3：实现 7 天撤销、旧 ID 跳转、外部修改冲突拒绝和别名环路检测。

### P2：后续产品阶段

- M10：版本化备份/恢复、损坏检测、导入预览、冲突策略，再评估 WebDAV；
- M11：RSS/Atom/OPDS 只读目录和授权提示；
- M12：TTS、漫画增强和音频书源能力评估，先做独立模型与性能基线。

## 当前大纲需要补充的横向能力

1. **验收证据账本**：每个阶段同时记录提交 SHA、CI run、产物 digest、人工记录和已知差距。
2. **迁移安全线**：任何改变书籍生命周期、章节归属或进度语义的迁移，都必须先有备份/快照、失败回滚和旧库夹具。
3. **兼容性分级**：继续区分“可执行”“可导入但不执行”“明确拒绝”“暂不支持”，并同步维护兼容性矩阵。
4. **升级兼容**：每次安装包发布都验证旧数据库升级、降级/回滚提示和用户数据保留。
5. **可访问性**：键盘导航、焦点可视化、窄窗口、中文字体、高对比度和缩放进入 Windows 手工验收。
6. **性能基线**：书源批量写入、书架查询、图片复核、TXT/EPUB 大文件和缓存清理均保留可重复阈值。
7. **隐私与授权**：不保存 Cookie/Authorization/Referer，不执行未知脚本，不上传正文和未授权内容；日志继续脱敏。
8. **文档一致性**：专项计划、路线图、兼容性矩阵和发布清单必须在每个里程碑后同步更新。
9. **CI 运行时维护**：当前 Actions 仍出现 Node.js 20 弃用警告；不阻塞本次通过，但需要后续升级 action pin 或运行时配置，并在升级后重新验证 Windows Release。

## 后续执行顺序

1. 完成本次 M7.1d 文档同步并通过 CI。
2. 由用户在目标 Windows 环境完成 P0 手工验收并保存记录。
3. 在 d2 迁移评审中冻结 `0016` schema、默认策略、配额和失败回滚夹具。
4. 实现 d2 纯文本事务合并，再实现 d3 撤销和旧 ID 兼容。
5. 以 M10 版本化备份作为后续跨设备同步的前置，不提前引入 WebDAV 或自动上传。

## 完成定义

代码、Rust/前端 CI、Windows Release 和 installer smoke 通过只是必要条件；涉及安装、升级、字体、焦点、WebView2、路径恢复或数据迁移的能力，必须同时有目标环境人工记录。所有未实现的 Legado 能力必须在界面和文档中显式标注，禁止静默降级。
