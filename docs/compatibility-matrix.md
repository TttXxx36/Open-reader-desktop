# 书源与内容兼容性矩阵

兼容性按“已验证”而不是“理论上可解析”记录。测试内容必须是用户自有、已授权、公开测试或公版内容。每一行同时说明“导入状态”和“执行状态”，避免把导入成功误认为运行成功。

| 能力 | 目标版本 | 导入状态 | 执行状态 | 当前边界/下一步 |
| --- | --- | --- | --- | --- |
| 本地 TXT | MVP/M8.2/M8.4 | 已实现 | 已实现 | UTF-8、UTF-16、GB18030、章节识别、段落和进度；M8.2 已支持导入前预览、内置/不拆分/自定义正则章节规则、全角空格归一化、文本替换与编码提示；M8.4 首个切片已按行累积章节正文并通过约 1 MiB/512 章夹具（run 31328789321），并以单次字符扫描归一化 CRLF/CR 与全角空格（run 31329875168），无替换规则路径改为逐行处理且有效 UTF-8 借用解码（run 31330389289，95 个 Rust 测试）；1/16/64 MiB 尺寸矩阵每档设置 20 秒时间上限（run 31330961076）；默认无替换规则路径已验证 UTF-16LE/BE 与 GB18030 的 64 KiB 有状态分块解码（run 31332336289，97 个 Rust 测试，artifact 9043330887）；性能步骤已修正为运行完整测试路径，run 31334298865 通过，artifact 9043889018 记录 Ubuntu 上 37/560/2241 ms 与 13,369,344/63,385,600/239,243,264 bytes 峰值 RSS；同一 run 的 Windows 测试编译与峰值 RSS 采样测试通过；替换规则已采用 64 KiB 有界滚动缓冲并覆盖跨块匹配，M8.4 已收口，下一步转入 M8.5 |
| 本地 EPUB | MVP/M8.1/M8.3 | 已实现 | 已实现基础版 | EPUB 2/3 文本、目录、blocks-v1、受限图片；M8.1 已加入 ZIP 条目 2,048、单条 16 MiB、总量 64 MiB 和路径安全边界；M8.3 已保留安全内部链接元数据与安全块锚点，支持同章/跨章节片段跳转和四类有限内联 CSS 属性的双重过滤，未解析目标只展示索引；损坏 ZIP/container/OPF 明确报错，缺失单个 spine 章节可跳过；后续完善缺失资源提示、字体/CSS 规则扩展 |
| Legado 基础 JSON | M3/M6 | 已实现 | 已实现安全子集 | 支持项目包、对象、数组、BOM、包装对象、常见别名和 URL 预览 |
| Legado 书源元数据 | M7.0 | 已实现 | 已保存 | 支持 bookSourceUrl、分组、类型、权重、发现开关、自定义顺序、备注和 URL 模式；音频类型明确拒绝 |
| 书源管理元数据 | M7.1a/b/c | 已实现 | 已实现基础管理 | SQLite 迁移、旧配置回填、分组筛选、排序、搜索/发现开关、元数据编辑、多选批量操作、同分组上下移动、批量分组移动、导入 diff、更新/跳过/新建冲突策略、导入前快照和原子恢复已验证；拖拽、批量备注/权重编辑和 Windows 手工流程待完善 |
| CSS/JSoup 选择器 | M3/M7 | 已实现 | 已实现 | 支持文本/属性提取、单选择器和规则别名；不执行脚本表达式 |
| 正则替换/提取 | M3/M5 | 已实现 | 已实现受限版 | 首个捕获组和 replaceRules；有数量、长度和超时边界 |
| JSONPath | M3/M7 | 已实现 | 已实现安全子集 | 支持对象字段、数组下标、数组/对象通配、连续括号字段别名、单字段等值过滤和 json: 前缀；路径/字段/值/节点数有上限，不等式、逻辑组合、函数和任意脚本拒绝 |
| 分页变量/模板变量 | M7.2 | 已实现受限版 | 已实现首个切片 | 支持 page/pageNum/pageIndex/page+1/page-1、keyword/key、bookUrl/bookId/chapterId；多页最多 20 页，空页/无新增结果终止；调试变量脱敏展示；后续补解析数量、终止原因和链式规则 |
| 链式规则/多级 URL | M7.2 | 已实现安全子集 | 已实现安全子集 | CSS/JSONPath 规则链最多 8 项；每个请求阶段的 URL 回退链最多 8 项，失败候选保留在诊断步骤并可随快照导出，成功 URL 用于相对章节链接；阶段共享最多 60 秒预算，端到端 pipeline 总预算最多 120 秒，调试/远端/多源请求均支持取消；混合表达式、脚本和 XPath 拒绝；正文支持受限 nextUrl/nextPage 中间模型（单个绝对 HTTP(S) 链接、最大 2 KB）；纯策略闸门已覆盖候选校验、同源、深度/页面/响应体/总耗时、环路与稳定 stop reason（60 个 Rust 测试，run 30772265421），累计多页夹具验证预算不按页重置、深度优先级稳定（61 个 Rust 测试，run 30772819136），仅在 `NextPagePolicy.enabled=true` 时的三页合成 HTTP 请求链已通过（62 个 Rust 测试，run 30773702655）；部分成功、环路、跨源、超时和超体积边界也已通过（67 个 Rust 测试，run 30774860219）；默认阅读路径仍不自动追链；`fetch_source_chapter` 已接受可选 `next_page_policy` 并复用取消 token（run 30775360305），设置页已提供追链开关、配额控件和同源限制；远端阅读页显示追链停止原因，取消按钮继续复用 operation token，启用策略时跳过旧单页缓存；首轮 UI 已通过 run 31302166671；诊断导出现在追加 `next_page_policy` 安全元数据（配额、同源开关、停止原因和状态文案），run 31302984553 通过；Windows 手工验收仍待执行（见 m7-next-url-policy.md） |
| XPath 选择器 | M7.3 | 明确标记（预览保留原表达式） | 暂不执行 | 规则上下文静态识别最多 8 条、每条最多 512 字节，展示路径、不执行原因、静态解析状态、步数、估算工作量和离线解析耗时；离线 PoC 仅解析受限路径/谓词并统计合成 HTML 节点，夹具覆盖 6 条受限语法、6 条拒绝语法、授权耗时分布及尾部分隔符/组合谓词/节点预算回归，70 个 Rust 测试通过（run 31307700513），仍不翻译为 CSS、不访问网络 |
| Legado JavaScript | M7.4 | 明确拒绝 | 不执行 | 需要受限运行时、超时、堆限制、API 白名单、审计和许可证评估 |
| Cookie/Authorization/登录态 | 治理项 | 明确拒绝 | 不执行 | 不自动携带敏感头；若未来支持必须逐项授权并使用安全存储 |
| 多源搜索、去重和失败隔离 | M5/M7.5a/b/e/f/g/h/i/j | 已实现 | 已实现 | 并发上限、单源失败隔离、标题/作者归一化和诊断日志；失败项支持只重试指定书源，按来源 ID/标题/作者合并结果并保留其他来源状态；SQLite 0008 保存搜索失败的有限脱敏摘要，0009 增加可选 operation_id，书源页支持筛选/刷新/清空和 64 条本地失败报告导出；原因码固定为 timeout、cancelled、rate_limit、auth、http_status、body_limit、parse、config、network、request 并有回归测试；0008 → 0009 旧库升级夹具已通过；报告 schema_version 兼容约定见 [报告格式文档](source-failure-report-schema.md)；source_metrics 现同时显示总/启用源、审计摘要、失败记录、缓存占用，以及按来源/阶段的请求尝试、成功、失败、缓存命中和明确分母的失败率/缓存命中率；规则指标已落地 SQLite 0011/0012、四个解析阶段和字段级 rule_key；no-match/failure 进入 attempts 分母，skipped 单独计数且不进入分母，由 [规则执行指标边界](source-rule-metrics.md) 固定缓存/取消边界与隐私约定；当前 UI 已展示总量、比例、skipped 和按规则分解；取消请求不计成功/失败，默认不收集遥测 |
| 缓存、章节指纹和 stale 回退 | M5/M7.2/M7.5c/d/e/f/g/h/i | 已实现 | 已实现 | TTL、容量上限、过期清理和刷新失败回退；书籍/章节返回 cache_hit，阅读器显示缓存来源；脱敏诊断快照已合并 pipeline、书籍/章节请求步骤、缓存回退事件与多源失败摘要，并提供相对 order/start_ms 排序；SQLite 0008/0009/0010 保存搜索、book、chapter 阶段的有限失败摘要、请求/缓存聚合与跨请求 operation_id，取消不记失败；历史按时间保留最近 512 条，书源页提供按原因/阶段统计、请求失败率/缓存命中率和脱敏报告导出；缓存命中率分母为已完成网络请求次数加新鲜缓存命中次数，stale 回退不计命中；规则指标设计明确缓存命中和 stale 回退均不重复执行规则；旧库升级夹具已覆盖 0008 → 0009，0010 在启动时自动迁移；报告 schema_version 兼容约定已记录 |
| MOBI/AZW | M8 | 暂不支持 | 暂不支持 | 先做只读解析 PoC，确认许可证、内存和大文件策略 |
| PDF | M8 | 暂不支持 | 暂不支持 | 独立评估渲染、搜索和目录模型，不转成普通文本章节 |
| 漫画/图片 | M8/M12 | 暂不支持 | 暂不支持 | 独立评估缓存、缩放、双页和阅读方向 |
| 音频书源/TTS | M12 | 暂不支持 | 暂不支持 | 先做播放器、系统 TTS、隐私和缓存评估；bookSourceType=1 保持拒绝 |
| RSS/Atom/OPDS | M11 | 暂不支持 | 暂不支持 | 先做订阅/目录模型和授权提示，不聚合未经授权内容 |

## 兼容等级

- **已实现**：有 Rust/前端测试或授权夹具，且远程 CI 通过。
- **可部分导入**：可保存一部分安全字段，但会明确显示未实现能力；不保证端到端执行。
- **明确标记/拒绝**：预览中显示原因，严禁静默丢弃脚本、XPath、认证和音频能力。
- **暂不支持**：不提供导入承诺，进入对应里程碑后再做格式或运行时评估。

## 兼容原则

1. 先定义内部数据模型，再做字段映射；不为兼容而复制不安全行为。
2. 每支持一个字段，必须补充最小测试夹具、错误案例和迁移说明。
3. 网络请求、重定向、响应体、缓存、图片、脚本和请求头均有上限。
4. 真实站点只在用户确认授权、服务条款允许且不绕过访问控制时测试。
5. 兼容性矩阵随每个里程碑更新，并链接到对应 GitHub Actions 运行记录。
